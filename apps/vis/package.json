{
  "name": "@cloud-code/vis",
  "version": "0.1.0",
  "private": true,
  "description": "Debug visualization tool for cloud-code sessions",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:deps": "pnpm --filter @cloud-code/agent-core... build",
    "predev": "pnpm run build:deps",
    "dev": "node scripts/dev.mjs",
    "build": "pnpm run build:deps && pnpm --filter @cloud-code/vis-server build && pnpm --filter @cloud-code/vis-web build && node scripts/copy-web-dist.mjs",
    "prestart": "pnpm run build",
    "start": "node server/dist/server.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
