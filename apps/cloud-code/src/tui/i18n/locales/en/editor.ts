/** See common.ts for contribution rules. */

export const editor = {
  // ── Input editor ──
  'editor.shellMode.label': '! shell mode',
  'editor.placeholder': 'Type a message (? for shortcuts)',

  // ── Autocomplete ──
  'editor.autocomplete.noMatch': '  No matching commands',
} as const;
