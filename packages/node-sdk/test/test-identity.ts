import type { CloudCodeHostIdentity } from '#/index';

export const TEST_IDENTITY: CloudCodeHostIdentity = {
  userAgentProduct: 'cloud-code-cli',
  version: '0.0.0-test',
  platform: 'kimi_code_cli',
};
