{
  "name": "@cloud-code/minidb",
  "version": "0.2.1",
  "private": true,
  "description": "A pure-Node.js embedded key-value database mixing Redis-style in-memory KV with SQLite-style durable persistence (WAL + snapshot).",
  "license": "MIT",
  "author": "Moonshot AI",
  "homepage": "https://github.com/yspbwx2010/cloud-code/tree/dev/packages/minidb#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yspbwx2010/cloud-code.git",
    "directory": "packages/minidb"
  },
  "bugs": {
    "url": "https://github.com/yspbwx2010/cloud-code/issues"
  },
  "keywords": [
    "kimi",
    "database",
    "key-value",
    "embedded",
    "wal",
    "snapshot"
  ],
  "files": [
    "dist"
  ],
  "type": "module",
  "imports": {
    "#/*": "./src/*.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./cluster": {
      "types": "./src/cluster/index.ts",
      "default": "./src/cluster/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "bench": "node --import tsx bench/bench.ts",
    "bench:cluster": "node --import tsx bench/cluster.ts",
    "clean": "rm -rf dist"
  }
}
