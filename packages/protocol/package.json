{
  "name": "@cloud-code/protocol",
  "version": "0.2.1",
  "private": true,
  "description": "Shared REST + WS protocol schemas (envelope, error codes, pagination, ws-control) for the Cloud Code CLI daemon.",
  "license": "MIT",
  "author": "Moonshot AI",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yspbwx2010/cloud-code.git",
    "directory": "packages/protocol"
  },
  "bugs": {
    "url": "https://github.com/yspbwx2010/cloud-code/issues"
  },
  "type": "module",
  "imports": {
    "#/*": "./src/*.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ulid": "^3.0.1",
    "zod": "^4.3.6"
  }
}
